# 金鸡湖地图推荐区域 Demo：项目上下文

更新日期：2026-06-11

## 项目背景

酒店列表页地图 Demo，用于演示“景区 POI 围栏 + 推荐居住区域 + 酒店价格气泡”。视觉参考 Figma「个人 小程序主流程 25-26年」，业务场景位于苏州金鸡湖。

## 需求目标

- 还原移动端地图搜索与筛选界面。
- 使用真实国内地图底图。
- 展示景区围栏、互不重叠的推荐区域围栏和推荐气泡。
- 支持点击推荐、手动缩放、缩小退出和重新进入。
- 酒店气泡覆盖全图、均匀分散、互不重叠。
- 放大后所有推荐区域都有酒店。
- 支持手机、桌面和公网评审。

## 已完成

- 接入高德 JavaScript API 2.0，支持拖动、滚轮、触摸和按钮缩放。
- 实现 S1、S2、S2M、S3 四态状态机。
- 修复缩小退出后重新放大无法恢复推荐区域的问题。
- 手动放大与点击推荐区域共用聚焦逻辑。
- 实现金鸡湖蓝色景区围栏和三个橙色推荐区域围栏。
- 推荐卡片居中，优先于酒店气泡显示。
- 酒店通过固定种子生成、网格抽样和矩形碰撞检测均匀展示。
- S2 放大态为所有推荐区域加载区域酒店。
- 已部署到 Vercel：https://suzhou-map-demo.vercel.app/

## 关键决策

- 使用高德地图，保证国内道路、POI 和坐标表现。
- 高德负责底图和投影，DOM/SVG 负责业务视觉。
- GitHub 保存源码，Notion 保存方法论与上下文，Vercel 提供无需本地服务的演示地址。

## 踩坑经验

- 浏览器缓存可能造成新脚本配旧样式，静态资源需使用版本查询参数。
- `file://` 下第三方地图与缓存行为不稳定，应使用 HTTP 或 Vercel。
- 酒店不能按数组顺序直接渲染，需要扩大经纬度范围、网格抽样和全状态碰撞。
- Vercel 默认部署保护需要关闭；国内网络访问 `vercel.app` 仍可能不稳定。

## 待办事项

- 用用户提供的橙色图钉切图替换当前 SVG。
- 将 `.search-sheet` 四角统一为 16px。
- 继续按最终 Figma 微调金鸡湖围栏轮廓。
- 在高德控制台配置域名白名单并补充 `securityJsCode`。
- 评估自定义域名或国内静态托管。
- 视需要将历史仓库名 `web_Photo-wall_demo` 改为地图项目名称。

## 跨设备继续入口

- GitHub：https://github.com/56216394/web_Photo-wall_demo/tree/codex/map-demo-handoff
- Vercel：https://suzhou-map-demo.vercel.app/
- 主要文件：`index.html`、`styles.css`、`script.js`
- 方法论：`PROJECT_METHODOLOGY.md`

换设备后建议依次读取：本上下文 -> 方法论 -> GitHub 源码 -> Vercel 当前效果 -> 待办事项。
