# 地图推荐区域 Demo：方法论与技能沉淀

## 适用场景

适用于把 Figma 地图方案快速转成可交互 Demo：真实底图上叠加景区围栏、推荐居住区域和酒店价格气泡，并支持桌面、手机与公网评审。

## 核心方法

### 1. 先拆状态，再写视觉

- S1：景区与推荐区域概览。
- S2：推荐区域聚焦，显示区域酒店，不再显示推荐名称气泡。
- S2M：多推荐区域展开。
- S3：普通酒店浏览。

点击、手动缩放、缩小退出与重新放大都必须有明确状态迁移，避免内容无法恢复。

### 2. 真实地图承载，自定义业务覆盖物

高德 JS API 2.0 负责瓦片、拖动、缩放和坐标投影；SVG 负责围栏；DOM 负责酒店气泡、推荐卡片、POI 和图钉。这样既保留真实地图交互，也便于按 Figma 调整视觉。

### 3. 经纬度实时重投影

地图移动或缩放时，使用 `lngLatToContainer` 将经纬度转为屏幕坐标，通过 `requestAnimationFrame` 合并连续渲染，使围栏和气泡始终跟随底图。

### 4. 显式定义碰撞优先级

展示优先级：推荐区域围栏和推荐卡片 > 景区图钉与 POI > 酒店气泡。酒店先做可视范围过滤，再排除推荐卡片保护区，按屏幕网格抽样并做矩形碰撞检测，同时保证每个推荐区域至少有酒店。

### 5. 使用稳定随机数据

使用固定种子的伪随机数生成全图酒店和区域酒店。数据看起来随机，但刷新后保持一致，便于评审和复现。

### 6. 视觉还原循环

按“读取 Figma 尺寸 -> 实现 -> 目标视口截图 -> 对比锚点、尺寸、层级、遮挡 -> 修正”循环。静态且要求 1:1 的图形优先使用设计切图；需要动态换色和缩放时使用 SVG。

## 技能清单

- Figma 节点尺寸、层级和视觉结构读取。
- 多张静态稿抽象为状态机与交互规则。
- 高德地图初始化、事件监听和坐标投影。
- SVG 多边形、DOM 覆盖物与层级管理。
- 点在多边形内判断、固定种子随机数、矩形碰撞、网格抽样。
- 本地静态服务、局域网访问、Vercel 部署、GitHub 分支和 Notion 知识交接。

## 关键经验

- 地图 UI 的核心是状态、投影、层级和碰撞同时正确。
- `file://` 不适合作为正式打开方式，应使用 HTTP 或部署地址。
- 高德 Web Key 会出现在客户端代码中，必须配置域名白名单并补齐 `securityJsCode`。
- 国内网络访问 `vercel.app` 可能不稳定，可准备自定义域名或国内静态托管。

## 验收清单

- 底图可拖动、滚轮缩放和触摸缩放。
- 围栏与气泡跟随地图同步移动。
- 推荐区域互不重叠，卡片位于区域中心附近。
- 酒店气泡互不重叠且位于围栏上方。
- 放大后每个可见推荐区域都有酒店。
- 缩小退出后重新放大，推荐内容可以恢复。
- 手机和桌面无文字溢出、控件遮挡。
- 公网地址无需本地服务即可访问。
